#!/usr/bin/env bash
set -euo pipefail
cargo fmt --check
cargo check --locked
cargo check --locked --no-default-features
cargo check --locked --all-features
cargo check --locked --features dev

inherited_lints=(
    # 2 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::chunks_exact_to_as_chunks
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::cloned_ref_to_slice_refs
    # 23 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::doc_overindented_list_items
    # 7 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::double_ended_iterator_last
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::drain_collect
    # 10 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::io_other_error
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::large_enum_variant
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::len_zero
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::manual_checked_ops
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::manual_clear
    # 7 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::manual_div_ceil
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::manual_is_multiple_of
    # 4 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::manual_repeat_n
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::manual_saturating_arithmetic
    # 2 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::needless_borrows_for_generic_args
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::neg_multiply
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::question_mark
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::redundant_pattern_matching
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::repr_packed_without_abi
    # 3 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::result_large_err
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::unbuffered_bytes
    # 11 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::unnecessary_sort_by
    # 3 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::useless_borrows_in_formatting
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::useless_conversion
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::useless_format
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A clippy::vec_init_then_push
    # 5 inherited files; upstream clippy repair — orchestrator files the Story.
    -A dead_code
    # 2 inherited files; upstream clippy repair — orchestrator files the Story.
    -A mismatched_lifetime_syntaxes
    # 1 inherited files; upstream clippy repair — orchestrator files the Story.
    -A unused_imports
    # 2 inherited files; upstream clippy repair — orchestrator files the Story.
    -A unused_parens
 )
cargo clippy --locked --workspace --all-targets -- -D warnings "${inherited_lints[@]}"
(cd crates/client-wasm && wasm-pack build --target web --locked)
(cd frontend && npm ci && npm run check && npm run lint)
