# raised from 100 on 2026-09-10 — clippy 1.98 counts 104 and 106 lines in two inherited test fns (`sitemap.rs::parse_sitemap`, `backlink_grouper.rs::test_grouped_backlinks`); the crate-level `#![warn(clippy::too_many_lines)]` makes a CLI `-A` ineffective; lower again in the upstream clippy repair Story.
too-many-lines-threshold = 110

